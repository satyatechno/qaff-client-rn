export const FETCH_PROPOSALS = (data) => ({
  type: 'FETCH_PROPOSALS',
  data,
});
export const FETCH_MORE_PROPOSALS = (data) => ({
  type: 'FETCH_MORE_PROPOSALS',
  data,
});
export const CREATE_CONTRACTS = (data) => ({
  type: 'CREATE_CONTRACTS',
  data,
});

export const SHORTLIST_UNSHORTLIST = (data) => ({
  type: 'SHORTLIST_UNSHORTLIST',
  data,
});
