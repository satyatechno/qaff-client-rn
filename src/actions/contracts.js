export const FETCH_CONTRACTS = (data) => ({
  type: 'FETCH_CONTRACTS',
  data,
});
export const FETCH_MORE_CONTRACTS = (data) => ({
  type: 'FETCH_MORE_CONTRACTS',
  data,
});
// export const FETCH_CONTRACT_DETAILS = (data) => ({
//     type: 'FETCH_CONTRACT_DETAILS',
//     data,
// });

export const GET_CONTRACTS = (data) => ({
  type: 'GET_CONTRACTS',
  data,
});
